# 后续工作交接

交接日期：2026-09-16（Asia/Shanghai）。用户已明确授权把后续工作转至「验证子智能体只读检查」。新任务已成功启动 gpt/gpt-6-astra 子智能体 Hooke，执行只读工具并关闭。

原任务：执行大学生职业规划智能体完整实施与验收计划，不是仅讨论方案。持续推进至完成，真实 API 不可用时如实保留验收阻塞。新任务与旧任务共享同一目录，无需复制或重新搭建。

- 项目：C:/Users/Administrator/Desktop/智能体
- 原始完整计划：C:/Users/Administrator/.codex/attachments/933bc638-5ce9-4e96-af9d-5e18b1f80ff3/pasted-text.txt
- 原任务 ID：01a0a5bc-3dbc-77b0-be0b-6ccd02826607
- 接收任务 ID：01a0a76c-be0e-7b42-addf-c8f2e0276ceb
- PowerShell、Python 3.12 .venv、Node/npm 已可用；目前无 Git 仓库。

## 必须保持的范围

React/TypeScript/Vite/Recharts 前端，FastAPI/Pydantic/HTTPX 后端；依据原 XLS 建立六类岗位画像，职业晋升和换岗路径，手动能力录入和 PDF/DOCX/TXT 简历预填，确认画像，基础/增强匹配，筛选推荐，真实可配置兼容 Chat Completions 服务，TXT 导出。本机 127.0.0.1:8000 单地址服务，开发代理5173。无账号、数据库、OCR、部署。用户资料仅会话内存，不持久化简历或正文日志。

基础分=已确认、有证据且等级>0的必需标签数/必需标签总数；多余能力不稀释，重复只计一次，优先项不入分母。无证据/未确认待确认，明确等级0为差距。增强技能 min(学生等级/要求等级,1)，原文了解1/熟悉2/熟练3，无原文才默认2；关联最多0.25、不累加、不能说已经掌握。证书/素质二元。无要求维度不适用，整体无要求不可计算且排除推荐。推荐最多5个，城市/薪资筛选关联招聘记录，日薪/月薪不换算；无薪资过滤保留未知，技能全部包含，稳定岗位ID打破同分。

六种子代理角色与当前模型（2026-09-17 实测，四条路由均可返回）：data-explorer/volcengine-agent-plan/glm-5.3-flash、profile-engineer/deepseek/deepseek-flash、matching-engineer/deepseek/deepseek-flash、ai-engineer/Kimi/glm-5.3、viz-builder/Kimi/kimi-k3、quality-reviewer/volcengine-agent-plan/glm-5.3-flash；最多同时3个。选型依据与探针记录见 docs/代理角色清单.md。用户已经授权委派，不要另建用户聊天代替子代理。不要把失败代理说成独立审查成功。主控负责架构和最后验收。

## 已实现及最近验证

### 匹配

backend/app/matching.py、models.py；tests/test_matching.py。

匹配代理先完成83项测试，含27个独立手算案例、合成4项满足2项=50%、别名重复、熟练度、关联上限、多关联取最大、维度空、推荐排序与薪资城市技能过滤。

主控已修改 ALGORITHM_VERSION=matching-2.1：确认但无证据的正能力 -> pending/missing_evidence；关联源也必须有证据；重复记录优先有证据者；明确0无需正能力证据。新增6项回归用例。最近实际运行：

    .venv/Scripts/python.exe -m pytest tests/test_matching.py -q
    89 passed in 0.29s

注意：models.py 的字段说明、matching.py 顶部docstring还有“证据不参与评分”等旧描述，需要与最新逻辑同步。后端 API 已强制整体确认，纯匹配函数按条目确认。

### AI 与API

主控重写 backend/app/llm.py、main.py、run.py，修改resume.py。

llm.py：HTTPS或本机HTTP；服务端配置三个LLM变量；HTTPX timeout45；只对ConnectError/ConnectTimeout/429/5xx重试一次，read timeout不重试；不跟随重定向，错误不泄露上游正文；严格Pydantic输出和证据校验；系统Prompt把简历/岗位/经历全部作不可信数据。

注意待修：HTTPX timeout45是阶段/读间隔限制，尚未增加每次请求asyncio.timeout(45)总时限。配置示例有LLM_TIMEOUT但实际硬编码45，应按计划明确45并消除误导，或受控配置且上限45。

画像模型输出只允许 strength_tag_ids 和 improvements。优势ID必须来自既有已确认、有证据、非零能力；优势文本、原文引用由程序生成。保留原输入，整体 confirmed=False。

报告模型输出 focus（补充证据/加强实践/持续深化）和 activities[{tag_id,steps}]；允许ID由确定性差距/待确认/弱项计算；程序生成分数、契合度计数、方向标签；模型给未来活动。拦截明显已掌握/保证录用/百分比分数/注入表述。此为有限规则防护，不能夸称语义幻觉彻底消除。无真实API运行证据。

简历模型抽取 major/experiences/skills/certificates/qualities；ID维度和逐字证据校验；Java不匹配JavaScript；否定、计划、注入文本不当作能力；预填 level1、confirmedFalse，需人工确认。未静默截断超长字段。

main.py：统一错误 {error:{code,message,retryable,request_id}}，X-Request-ID/no-store；输入限制一般JSON1MB、上传5MB+multipart envelope；multipart内存阈值提升至限额，close释放；通用错误日志只有ID和异常类；PROFILE_UNCONFIRMED实际400（不是409）。/api/tags、/api/sources/{id}、jobs/detail/paths、profile/resume/matches/recommendations/reports。

报告服务端重算匹配，返回export_text、status complete、mode live、generated_at、输入/算法/数据版本。前端尚未接上export_text，见下。

run.py 支持任意工作目录，读取根.env，验证PORT，绑定127.0.0.1，明确端口占用，关闭access log。frontend/dist存在时由FastAPI静态提供。

resume.py：5MB/30000字符/50页；UTF8 BOM/GB18030，拒绝二进制控制符；pypdf内存读、加密/无文字/损坏反馈；python-docx内存读，文档XML10MB/ZIP总20MB限制，禁DTD/entity，含表格文字。

主控新增 tests/test_ai.py、test_api.py、test_resume.py（全部mock明确，不是真调用）：

    .venv/Scripts/python.exe -m pytest tests/test_ai.py tests/test_api.py tests/test_resume.py -q
    48 passed, 2 warnings in 0.62s

两个warning为Starlette TestClient/httpx和anyio弃用提醒。大字节参数自动pytest ID曾导致Windows环境变量过长，现在 test_bad_files 已加短ids，已修好。不要回退。

### 数据

scripts/build_data.py、scripts/数据字典.md、backend/data/career-data.json，版本career-1.1。原XLS只读，9958行，527完全重复行，780重复code行，14.8MB。当前生成6画像、每个40记录共240、18路径节点、24边（12换岗，含计划固定6条），35必需标签；内置verify先前通过，但部分检查本身违背计划，必须修正。

**尚未实施的数据修正（优先级高）：**

1. required_level全部固定2，wording_level只是观测。须使明确了解1/熟悉2/熟练3实际评分，无明确才默认2。多样本聚合规则要明确，取观测众数等可解释规则，并把用于等级的witness纳入evidence。当前wording_level在30字符内优先最强词，会错配邻近技能，应最近前置措辞/分句。
2. build_jobs排除非month/day（包括unknown/negotiable），不符合无薪资筛选允许未知的规则。
3. used_codes排除同编码不同内容版本。需要保留差异且不猜时间新旧；可以有限画像证据，但必须留版本索引与可追溯记录。不得把高级版本作为初级核心证据。
4. 先clean再dedupe可能合并原文不同记录；应该原始全字段签名去重再清洗。保留原始行号及hash/版本关系。
5. preferred_skills目前只写dimensions，真正preferred仅证书，需恢复技能优先项进入preferred，不入必需分母。检查核心要求的证据不能全为“优先/否定”。
6. 证书分类代码必需不少于2份，但实际只写preferred，且无关公司词过滤粗糙，需核对。未提及不补造证书，公司认证不计入个人。
7. SENIORITY有漏掉3-5年等表述可能，需审查原始证据。
8. SQL pattern目前 SQL|数据库 会把MySQL和任何数据库识别SQL；词典alias把数据库等于SQL有风险；api-testing别名把Postman/JMeter直接当接口测试等，应按证据审查避免工具泛化为能力。相关基础表只有主控审定4项。

build_data.verify内“全是默认2”“优先项仅证书”“code全部唯一”“薪资只能month/day”等检查需按正确计划改写。tests/test_matching.py尾部PINNED_PROFILES/PINNED_PREFERRED也是旧生产契约，应先审查数据，再显式更新；不要为了旧测试保留错误规则。测试内FROZEN_PROFILES/27独立手算期望保持不变。

### 前端（原前端代理没交接完成，现已关闭，可重新分派）

React18.3.1、Recharts2.15.3、TS5.6.3、Vite5.4.11、Vitest2.1.8和package-lock。已有4页Jobs/Profile/Paths/Matches、CSS、单元测试、dist和旧截图。之前代理的test/build结果需最终复跑。

**主控最新实际读源码确认的阻断问题，尚未修：**

- ProfileTab没有完整画像确认操作！API现要求student.confirmed=True，因此主流程会400。需要明确“确认完整画像”操作，编辑任何字段撤销整体确认。
- TagEditor只展示证据图标，没有可编辑evidence。手动新增evidence=''，新评分会全待确认。必须证据输入/编辑。
- App.updateStudent不撤销整体确认，analysis/advantages会随旧输入残留。编辑后要失效。
- handleSubmit/handleResume无在途输入版本保护，返回会覆盖请求期间新编辑。应检查revision/request snapshot，并防并发画像/简历互相覆盖。
- 简历merge shallow复制后直接修改found对象，可能污染React状态；不能提升用户已填熟练度。已有专业/经历未覆盖提示条件恒false，需修。
- 手动标签为任意text.toLowerCase，没有字典选择，建议/api/tags做已知标签/别名归一，并区分三维；不明标签不冒充得分标签。
- 证书/素质没有显式不具备选择（当前只有勾选确认），可加0/1，未知未确认。
- MatchesTab前端hasTags门槛让零标签学生无法匹配，原计划含零技能。允许已确认的零技能画像并展示0/待确认。
- MatchesTab只能展示推荐最多5个的active，未单独计算目标岗位，目标岗位可能不在前5。需要确保可直接看目标岗位/api/matches，并允许按推荐项更换比较目标；报告job与意向目标语义清楚。
- MatchesTab.load和generateReport没有防在途切换输入/岗位的保护；当前报告stale是事后版本比较，但过时结果应不覆盖新态。
- 匹配后编辑输入要明确失效并禁生成旧状态报告；当前按钮只disabled reportLoading。
- 当前算法版本只从旧结果读取，服务重启升级后旧会话不自动识别；可在重新激活/请求health时核对版本。
- frontend/src/report.ts在客户端重新拼文和nowText，复制和导出时间可能不同。应直接使用服务端export_text，同一份文本；ReportResp类型加该字段。重写对应单元测试。
- 目前主控尚未改动前端，请新代理独占frontend，主控只管共享契约。

## 凭据与隐私

.env.example最初含真实硬编码密钥，主控已删去，现为replace-with-your-local-key。不要恢复或输出旧值；最终交付应提醒这把曾暴露的密钥轮换。之前未发现.env或LLM/OPENAI环境变量。只检查存在性，不打印凭据。

.env.example现配置 https://api.123uq.com/v1、deepseek-flash、timeout45、PORT8000；建议通用接口占位或清楚说明使用自己的可信兼容供应商。该现有地址不是必须使用的服务。

真实凭据只在本机配置，不要要求用户在聊天发key。没有真实API成功则P2真实调用门槛/P5最终验收未通过；其余代码与mock自动化可继续完成。

## 下一步建议与剩余交付

1. 接收任务先读完整原计划及本交接，确认工作目录和可用子代理。优先主控数据口径或前端阻断一项；明确不同文件所有权，最多3子代理并行。无需重做已经完成的模块。
2. 修复数据、前端上述问题，同步类型/文案；审定真实源记录的要求证据，生成数据可重复性检查（两次输出hash一致）。
3. 建立安装/启动PowerShell脚本（目前README/start/install尚不存在；早前尝试写入失败，不能当作已做）。使用PSScriptRoot/Set-Location，检查LASTEXITCODE；安装npm ci/build和venv；启动不必重复安装；清楚缺依赖/构建/端口错误。锁全传递Python依赖Python依赖锁定.txt；前端用package-lock。干净依赖环境安装并复验启动。
4. 虚构学生JSON + 实际TXT/DOCX/文本PDF样例；明确虚构、无真人信息。主控已读/宣布pdf和docx技能但尚未生成样例；需按技能制作并实际提取验证。
5. Playwright浏览器E2E，真实确定性后端+仅测试环境AI mock，不在生产加假成功模式。覆盖手动路线、简历路线、3格式、确认/证据、0技能、目标切换、分数/图表/推荐/报告版本一致、复制与下载逐字一致、旧结果失效、筛选和排序、错误保留输入、桌面+窄屏。保存截图并实际查看。旧截图不代表新API闭环成功。
6. 真实API smoke脚本仅读本机.env，调用health/profile/resume/report并输出脱敏结果，失败如实返回。无key时记未验证，不写成功证据。3格式本地抽取已测，真实模型预填仍待真调用。
7. README使用说明、评分口径、数据边界、模型Prompt/输出schema/异常、两条演示路线、15项验收矩阵、问题处理记录、测试报告和明确标注mock的报告样例。
8. 独立quality-reviewer复验且修复，主控最终全测试/build/E2E，并启动本机服务给用户。不要因token或时间停在“可继续”。外部凭据阻塞只限真实调用验收，其余授权工作都完成。

## 资源与工具

可用工具通过functions.exec内ALL_TOOLS发现，工具返回需要text(...)才能看到。tools.apply_patch只收一个字符串，以*** Begin Patch/End Patch包裹。写Python内容时用String.raw避免反斜杠换行变成语法错误。PowerShell原生路径操作，不跨shell拼删除命令。Start-Process后台必须WindowStyle Hidden。

已加载bundled依赖路径（可重新load_workspace_dependencies确认）：
C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe
C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules
C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe
含Playwright/docx/reportlab等可用时先确认。项目依赖python-docx==1.2.0已安装，Python依赖.txt已更新；llm等新源码已compile和测试。

技能：C:/Users/Administrator/.codex/skills/pdf/SKILL.md 与 docx/SKILL.md；前端需要时使用已安装frontend-design等技能，不要仅凭关键词一股脑全读。

## 交接边界

旧任务仅写入本交接并发送消息，不再修改项目实现。新任务可直接在共享目录继续。所有上述“待修”描述都是最新实际阅读或已验证状态，仍应以当前文件为准。最近两次通过的测试分别89+48，并不代表全计划验收通过。用户刚确认的Hooke成功是代理连通性验证，与真实应用LLM API验收是两回事。
