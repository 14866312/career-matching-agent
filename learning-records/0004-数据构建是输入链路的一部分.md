# 数据构建是输入链路的一部分

用户能够识别岗位资料输入、数据构建和页面输出都可能导致旧数据继续显示。精确结论是：修改原始资料后必须重新运行 scripts/build_data.py，更新 backend/data/career-data.json，再检查后端读取和前端展示。

**Evidence**：用户指出“修改资料没有正确输入，或者岗位要求没有正常输出”，并接受了先确认数据构建步骤的补充。
